import React from 'react';

export default class Cart extends React.Component {
    constructor(props){
        super (props);
    }
    add(product){
        alert('added');
    }
    render(){
        return null;
    }
}